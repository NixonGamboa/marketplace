{
  "name": "maui-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "MAUI monorepo — PWA customers, admin front, backend serverless",
  "scripts": {
    "install:pwa": "npm --prefix MAUI-PWA-customers ci",
    "install:admin": "npm --prefix maui-admin-front ci",
    "install:all": "npm run install:pwa && npm run install:admin",
    "build:unified": "npm run install:all && npm --prefix MAUI-PWA-customers run build:unified",
    "vercel-build": "npm run build:unified"
  }
}
